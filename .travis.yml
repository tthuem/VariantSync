dist: bionic

language: java 
jdk:
  - openjdk10
  - openjdk11

#-----start GUI testing stuff
services:
  - xvfb
#-----end GUI testing stuff
cache:
  directories:
  - "$HOME/.m2"
install: true
script:
  - mvn clean verify -e
