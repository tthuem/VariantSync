dist: bionic

language: java 
jdk:
  - openjdk11

#-----start GUI testing
services:
  - xvfb
#-----end GUI testing
cache:
  directories:
  - "$HOME/.m2"
install: true
script:
  - mvn clean verify -e
